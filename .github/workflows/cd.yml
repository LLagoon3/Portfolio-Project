name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, portfolio]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: bash scripts/deploy.sh
